## List of Places

Simple list of places to plan out the trip, optimised for mobile web browsing.

### Structure
The page is index.html + styles.css + app.js. Data is places.json and photos.json. 
Images are static files in images/. fetch loads the JSON; the rest is DOM + CSS (sticky nav, scroll-snap, localStorage for theme).
